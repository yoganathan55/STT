build:
  template_file: docker-build-base.tyml
  dockerfile: "Dockerfile.build"
  dockertag: "deepspeech-lib"
  metadata:
    name: "DeepSpeech Docker build"
    description: "Testing |docker build| of DeepSpeech build image"
